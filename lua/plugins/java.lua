return {
    "mfussenegger/nvim-jdtls",
    --    config = function()
    --        local config = {
    --        cmd = {},
    --        root_dir = vim.fs.root(0, { ".git", "mvnw", "gradlew" }),
    --}
    --require("jdtls").start_or_attach(config)
    --end,
}
